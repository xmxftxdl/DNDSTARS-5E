/**
 * 影舞者气上限。
 *
 * TEMP: 气系统调试期间固定为 99。原公式待平衡后恢复。
 */
export function maxQiForLevel(level: number): number {
  // if (level >= 50) return 6
  // if (level >= 40) return 5
  // if (level >= 30) return 4
  // if (level >= 20) return 3
  // if (level >= 15) return 2
  // return 0
  void level
  return 99
}
