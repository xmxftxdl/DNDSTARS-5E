# DNDSTARS-5E 交接说明

## 项目目标

本仓库从 `Stars App` 的稳定框架提交 `833d5bc` 创建，用于开发采用 D&D 5e SRD 规则的独立 VTT。
原项目继续维护自定义规则；本仓库不得把 D&D 5e 规则改动推回原仓库。

## 已继承框架

- DM/多玩家端口与 DM 权威消息流程。
- 地图、网格、Token、障碍物和坐标系统。
- 骰子动画、结果同步与战斗 Log。
- Headless DM、战斗阶段、Interrupt Queue 和 Action ACK。
- 职业、技能、特性、资源和职业专用 Action Registry。
- 角色 JSON、持久化、Vitest 与 Playwright E2E。

## 新规则边界

保留框架，逐步移除当前弓手、逐风者、影舞者、重炮手及自定义 AP/攻防差值规则。
D&D 5e 规则必须实现为独立 `RulesetAdapter`，不得重新硬编码进地图页面或通用 Headless 分发器。

首批规则模块：

1. 六项属性、属性调整值与熟练加值。
2. AC、生命值、先攻与速度。
3. 动作、附赠动作、反应、移动力。
4. D20 攻击检定、优势/劣势与伤害骰。
5. 豁免、状态、专注、借机攻击。
6. 短休、长休、生命骰和死亡豁免。
7. SRD 职业、法术、装备与怪物内容。

## 已确认规则版本

- 当前规则唯一基准为 D&D 5e 2014／SRD 5.1。
- 中文 SRD 规则文字采用项目自译版本，并按 CC BY 4.0 保留准确署名。
- 不得混入 SRD 5.2.1 的 2024 修订职业、专长、法术或战斗机制。
- 数据类型中的 `dnd5e-srd-5.2.1` 只用于识别并迁移旧存档，新数据统一写入 `dnd5e-2014-srd-5.1`。
- 战士核心包只保留 SRD 5.1 的核心职业与勇士范型。此前录入的非 SRD 子职、战技、摘要、专用资源与选择字段已从当前源码删除。第三方内容只能通过 Rules Plugin API 以命名空间形式注册；平台不得内置、公开索引或把第三方内容标成 SRD。房间级临时托管只允许由 DM 主动上传，并要求房主拥有分发权。

后续职业、法术、装备与怪物数据均须先核对是否包含在 SRD 5.1 中。核心包只能采用 SRD 5.1 内容；非 SRD 规则必须由独立第三方插件提供，并在界面显示插件来源。

## 房间大厅与端点分配

- 应用默认先进入统一中文大厅，不再由访问端口直接决定身份。创建房间者固定为 DM，并选择 `dnd5e-2014-srd-5.1`；加入者输入六位房间码后，由共享服务在房间容量内原子分配 `player1`～`player8`。
- 房间创建、加入、心跳和离开协议位于 `scripts/shared-server-core.mjs`。房主心跳超过 15 秒即视为离线，离线或已关闭房间拒绝新玩家；玩家席位超过 30 秒无心跳后可回收。同一浏览器重连会恢复原席位。
- `src/lib/roomSession.ts` 保存当前房间、角色和席位，`src/lib/sharedApi.ts` 会自动把房间码加入全部状态、图片与事件请求，因此地图、角色、骰子和 Headless 战斗仍沿用既有 DM 权威链路，但数据按房间隔离。
- 5273、5274 等端口都可以创建或加入房间，身份以房间会话为准。自动化回归可显式设置 `VITE_BYPASS_ROOM_LOBBY=1` 使用旧的固定端口模式；该开关不应作为正常玩家入口。
- 玩家创建或导入角色时会写入当前 `roomId` 与 `roomMemberId`；玩家角色页只展示本人在当前房间创建的角色，不能通过角色选择器查看其他玩家角色。
- DM 的“角色”页是只读房间成员名册，只显示已加入玩家、在线状态和各自创建的角色名。DM 端不再提供新建、导入、编辑、改血、显隐、休息或删除角色的入口；战斗中的 DM Authority 状态结算不受该界面权限调整影响。
- 房间会固定 Rules Plugin API V2 规则包清单。DM 可把单文件 bundle 上传到当前房间；服务端核对 SHA-256 后按房间保存，成员在心跳发现缺失时自动下载、再次核对插件 ID／版本／哈希、写入本机 IndexedDB 并激活。文件不进入公共索引或跨房间共享，但服务端已参与分发，房主必须拥有内容分发权。
- 插件通用特性使用完整命名空间 ID 保存在角色中。地图行动必须经过玩家请求、DM 房间白名单／版本／所有权／阵营／距离／行动经济 preflight、纯 Headless resolver、ACK 与共享快照应用。未在房间启用或哈希不一致的插件行动必须 fail closed。

## P0 稳定性与战役恢复

- 共享服务公开 `/api/meta`，返回协议版本、最低客户端协议、构建标识和进程启动时间。客户端固定检查 5273 权威端；旧进程没有该端点或协议不兼容时，DM 端会明确要求停止旧进程并重新执行 `npm run dev:dm`，玩家端会提示联系 DM。
- `src/lib/sharedResourceValidation.ts` 是所有共享状态读写的统一运行时边界。角色、地图、战斗、骰子、日志、中断和玩家动作队列均检查各自 envelope；插件的未来状态至少必须是 JSON 对象。旧 `enemyApByToken` 会在客户端和服务端双重迁移。
- 服务端对畸形 JSON 或结构损坏的状态 fail closed：上传不会覆盖最后一份好状态；磁盘坏文件会移入按房间隔离的 `quarantine`，客户端也保留有上限的本机诊断样本并显示中文提示。
- 应用外壳和每个路由页面都由独立 Error Boundary 包围。地图、角色、规则包或总览中的单页异常只替换当前区域，提供“重试此页面／重新载入／返回总览”，并记录有限的本机故障编号，不再让整个端变成白屏。
- DM 总览页的“战役安全与恢复”可创建手动快照、查看并还原最近恢复点、导出完整战役包、导入后预检再还原。服务端在共享状态改动前按两分钟节流自动快照，按房间轮转保留 10 个；还原前再建立 `pre-restore` 保护点。
- 完整 `.dndstars5e-campaign.json` 包含共享状态、地图图片、房间规则集信息和房间托管插件。导入会校验格式、SRD 5.1 规则版本、资源结构、图片上限及插件 SHA-256；只有当前房间 DM 可操作。恢复后的顶层时间戳会推进，避免旧客户端凭较新时间戳把恢复点反向覆盖。

## P1 房间管理与多端一致性

- 共享服务协议已升至 v3。每个共享资源都带 `_sync.schemaVersion/revision/writerId/writtenAt`；浏览器写入使用 expected revision 做 CAS，客户端同一资源的连续写入会排队，旧页签冲突返回 409 并触发权威状态重读，不能再靠最后写入静默覆盖。
- 删除共享资源会写入带修订号的 tombstone，而不是直接丢失版本历史；这样旧客户端不能在删除后用旧 revision 重新创建资源。战役导出会跳过 tombstone，战役恢复会继续推进当前 revision。
- `_all` SSE 信封包含房间内单调递增序号和服务实例 ID。客户端忽略重复事件，发现断号或服务重启后会触发全量资源恢复；DM 总览页提供资源 revision、冲突拦截、重复事件和断档恢复诊断。
- 房间身份恢复 V2 已建立账号／房间成员／账号角色库三层边界。恢复码可在新设备签发账号会话，房间按 `accountId` 恢复原 `memberId`；成员区分在线、暂时断线、主动离开和被 DM 移除。DM 可恢复被移除账号的加入资格，并显式修复旧角色归属；同名自动迁移已从 DM 名册删除。
- 玩家角色会自动写入去除房间字段的账号角色库。角色带入房间前必须通过 SRD 规则集、角色 schema、游戏协议和角色所需插件的版本／SHA-256／状态 schema 校验。当前服务端存储可在未来替换为云端账号提供者，房间与 Headless 战斗仍只消费已通过兼容校验的房间角色快照。详见 `docs/account-identity-and-character-vault.md`。
- DM 总览页现可复制 `?join=房间码` 邀请链接、锁定/解锁房间、设置 1～8 人容量、设置或清除加入密码、踢出成员，以及把 DM 权限安全转让给规则包已就绪的玩家。转让会原子交换 DM/玩家身份，双方后续心跳会更新本机会话。
- 房间在线判定已适配浏览器后台节流：5 秒客户端心跳使用 120 秒 DM 容错和 300 秒玩家席位容错；页面重新可见、获得焦点或网络恢复时立即补发心跳。一次暂时的 `room-offline` 只进入重连状态，不再清除玩家房间会话；DM 显式离开仍会立即关闭房间。
- 真实房间不再初始化或同步 `sample-*` 旧展示角色；本地持久化升级到 v21 后会剔除这些记录，DM 载入旧共享快照时也会清理并回写。地图“角色…”放置菜单按当前房间名册过滤，只显示仍属于房间成员的角色；离房角色数据保留在战役存档中但不再污染放置列表。

## P2 规则包演进基础

- Plugin API V2 清单现支持 `stateSchemaVersion`；旧包按版本 1 处理。升级包可声明连续的纯 JSON `migrations`，每一步只允许 `n → n+1`，并且只能在已锁定 DOM、网络、浏览器存储和内部 Store 的 Worker 中执行。
- DM 发布改为“按哈希暂存 → 读取旧状态 → Worker 迁移预检 → CAS 原子激活”。服务器同时核对房间 `rulesRevision`、旧插件版本／哈希／状态 schema 和暂存哈希；任一冲突、缺失迁移、超时、降级或无效 JSON 都会保留旧规则包与旧状态。
- 房间插件文件改为内容寻址保存，激活时只切换房间清单指针；玩家仍通过心跳自动下载并校验完全一致的版本、SHA-256 和状态 schema。本机替换失败时加载器会从旧固定字节恢复旧 Worker。
- 插件运行状态随房间战役 manifest、快照和导出进入恢复边界；卸载停止分发但保留该插件的命名空间状态，以便重新安装或后续升级。
- 房间密码只保存加盐 scrypt 摘要，预览与名册仅公开“是否需要密码”；锁房只阻止新成员，原成员仍可重连。加入页会显示 DM、在线状态、人数/容量、锁定状态及插件名称、版本、发布者和许可证。
- DM 上传、升级或停止分发房间规则包前，服务端会先创建 `pre-plugin-change` 快照。该保护点与手动/自动/恢复前快照共享房间级轮转策略。
- Headless action 可声明有限数量的骰子配方；所有骰面由 DM Host 骰子层生成，公开骰同步房间、暗骰只向 DM 显示，Worker 收到的 `rolls` 会在受信任 Headless 层再次逐项校验。
- 通用插件特性支持圆形、锥形、直线和可旋转矩形范围模板。客户端只提交锚点和朝向；DM preflight 按最新地图占格、阵营、最大目标数重建 `targets`，不会采用玩家伪造的目标列表。
- 插件行动可声明 `interrupt` 及 actor／target／DM audience。选择进入共享 `plugin-choice` Interrupt Queue；同一 action ID 锁定一个事务，刷新或断线后继续读取，超时采用插件声明的默认项后再进入 Headless。

## P3 声明式扩展内容

- 插件可用 `registerSubclass` 为 12 个 SRD 基础职业声明通用子职、等级特性和多级选择组。子职、选择和自动授予特性全部使用插件命名空间；Host 按角色职业、所选子职与等级复核，自动特性不能在通用特性页手动勾选。战士插件子职同时接入现有战士进度面板，其余职业进入通用职业进度面板。
- 插件可用 `registerResource` 声明固定值或逐级上限、最低等级、所属职业／子职以及短休／长休恢复。资源进入通用 `classResources`，由角色同步和休息生命周期管理；Worker 只能请求 `spendResource`／`restoreResource`，受信任 Headless 层会再次检查命名空间、角色资格、余额和上限。
- 插件范围特性可声明 `persistentArea`。成功事务会在权威地图创建带来源、格子、颜色、轮数与可选专注 ID 的区域实体；固定时限到期或来源专注中断后由 DM Host 清除并同步。DM 规则包工作室可编辑首次创建／进入／回合开始／回合结束触发器、每轮去重、豁免、伤害、状态和 DM 调整；同一次移动反复进出不会绕过 `oncePerRound`。区域可放在当前没有生物的合法格子上，玩家提交的目标列表仍不会被信任。
- `phb-2014-compat-template.dndstars5e` 已扩展为不含专有规则正文的 P2／P3 示例，展示 Host 骰子、Interrupt、原创子职、选择组、职业资源与专注区域。

## SRD 职业接入状态

- `src/rulesets/dnd5e/classes.ts` 是 12 个职业、各职业唯一 SRD 子职、1–20 级特性、职业选择与施法进度的规则真相源。
- `src/classes/dnd5e/classDefinitions.ts` 把除战士外的 11 个职业注册到通用职业 Registry，并声明法术位、契约位及按短休／长休恢复的职业资源。
- 角色职业页会保存子职和职业选择；本地选择与等级都带有旧共享快照防回弹保护。
- 地图上的 5e 职业使用动作、附赠动作、反应和移动；通用武器攻击、职业资源消费与法术位资源都位于 5e Headless 边界，不读取旧 AP。
- 角色存档归一化会强制写入 `dnd5e-2014-srd-5.1` 并把兼容 AP 字段归零；旧存档中显式为空的 `rulesetId` 不再能把 5e 移动送回旧 AP 分支。共享战斗日志会迁移掉已经持久化的“花费／剩余 AP”文字。
- 已自动计算基础装备、AC、战斗风格、无甲防御、龙族韧性生命值、武僧／野蛮人速度与各职业额外攻击次数。双武器战斗的副手附赠攻击及伤害属性加值、防护战斗风格的 5 尺持盾反应与共享确认均已进入 DM 权威 Headless 结算。
- 勇士范型已接入 19–20／18–20 重击阈值、运动健将对地图先攻的半熟练、10 级第二战斗风格选择和 18 级 Headless 回合开始“生存者”治疗。运动健将的力量／敏捷／体质通用检定 helper 已存在；助跑跳远因地图尚无独立跳跃动作，仍需 DM 手动应用。
- 通用 `dnd5e-class-feature` 与武器攻击权威链已接入狂暴／狂乱／威吓气势／报复、鲁莽攻击、偷袭、巧妙动作、幸运一击的攻击侧、吟游激励、圣疗、至圣斩、神圣武器、神圣感知、净化之触、维持生命、武艺、疾风连击、疾风步、耐心防御、震慑拳、心如止水、空灵体、术法点转换、猎人巨像杀手／灭群者／巨人杀手反应／屠灭众敌，以及德鲁伊荒野变形、双生命池、结束形态和野兽动作。净化之触会由玩家选择接触目标身上的具体持续法术；多目标专注只移除所选目标，最后一个目标被移除时才结束来源专注。所有目标距离、阵营、职业等级、动作、豁免与资源由 DM/Headless 重新验证；客户端表单不能直接改血量或资源。
- 吟游激励持有者会在可能改变结果的攻击与豁免中收到共享选择提示；骰面、10分钟期限、一次性消耗和角色／未关联 Token 状态均由 Headless 持久化。不屈与金刚魂的失败豁免重掷已覆盖法术、震慑拳和专注豁免。
- 邪魔宗主“黑暗之主的幸运”已覆盖地图通用属性检定，以及法术、专注、震慑拳、散打技法、威吓气势、渗透劲、宁静心境与死亡豁免等 Headless 豁免：目标玩家通过共享中断决定是否加入 1d10，骰面与短休次数由 DM 端验证并消费。
- 14级邪魔宗主可在职业战斗栏预备“坠入地狱”；预备与取消不消耗动作，武器、借机或法术攻击未命中不会消费，命中后由 Headless 消耗长休次数并把目标放逐。目标状态会在角色与未关联怪物 Token 间持久化，直到邪术师下一回合结束才返回并结算预先由骰子盒掷出的 10d10 心灵伤害；邪魔类型返回但免除该伤害，返回伤害也会继续触发专注／坚韧狂暴豁免链。
- 11／13／15／17级邪术师可分别从 SRD 5.1 的完整 6／7／8／9环邪术师法术表选择一个秘法奥秘；21个候选均有中文名称并持久化。四个环级各有独立长休资源，地图只向已机械化的选择开放按钮，Headless 会重新核对职业等级、所选法术、法术环级和对应每日资源，绝不会用5环契约位代替。目前死亡法阵、死亡一指与律令死亡已接入；律令死亡按当前HP 100门槛直接判定死亡，不造成伤害、不会错误触发专注伤害检定或坚韧狂暴。死亡一指的人形生物僵尸生成以及其余复杂秘法奥秘仍由DM裁定。
- 已建立 `dnd5e-spell-cast` 多端 Headless 事务、人物卡法术选择与地图施法栏，目前共机械化57个法术；冷冻射线、电爪、雷鸣波与阳炎爆均已接入。通用 `timedEffects` 会随角色或未关联怪物 Token 持久化并由 Headless 回合节点推进：冷冻射线限制速度与疾走至施法者下一回合开始；电爪检查金属护甲攻击优势并封锁反应至目标下一回合开始；雷鸣波对失败目标执行经地图碰撞截短的10尺推离；阳炎爆处理亡灵／泥怪劣势、目盲攻防效果、十轮上限及每回合末重复豁免，自动与手动怪物回合也进入同一生命周期。此前新增的灼热射线、枯萎术、寒冰锥、连锁闪电、解离术和冰封法球（立即发射模式）继续沿用逐道攻击、特殊生物规则、范围模板与直接死亡结算。焰击术已支持火焰／光耀双伤害池、各自抗性与升环伤害类型选择；马友夫强酸箭已支持未命中半伤，并用原生 `ActiveEffectInstance` 在目标下一回合结束时自动结算后续强酸伤害。法术事务会消费动作／附赠动作及法术位／契约位／秘法奥秘，支持升环、多目标、命中、豁免、伤害、治疗与2014附赠动作法术限制。18级法师可选择一个1环与一个2环法术精通并按最低环级无限施放；20级可选择两个3环招牌法术，每个法术分别消费对应短休资源后免法术位施放一次；作为反应的精通护盾术也不会误扣法术位。专注来源、受术者与剩余轮数会通过角色／Token 状态持久化；虔诚护盾实际提供 +2 AC；祝福术／灾祸术支持升环多目标，灾祸术逐目标进行初始魅力豁免并逐个检查宁静心境。二者的1d4已接入攻击、普通法术与职业特性内嵌豁免、专注豁免及Headless死亡豁免，并由骰子盒提供、Headless校验。魔法飞弹在 UI 中逐枚分配、允许重复目标，并由 Headless 按飞弹顺序逐枚校验和结算；酸液飞溅支持相邻双目标与各自独立的敏捷豁免。恶言相加会让目标下一次攻击具有劣势；护盾术通过共享中断消费反应和最低可用法术位或法术精通，在施法者下一回合开始前提供 +5 AC 并免疫魔法飞弹，覆盖武器、借机、法术、怪物／野性变身多重、猎人多重与武僧连击。
- 标准状态效果引擎 V1 已建立：14 个 2014 标准状态使用稳定 ID 和中文显示名，`timedEffects` 支持多来源并存、固定轮数、双方下回合开始及目标回合末重复豁免。Headless 已统一处理速度归零、失能动作／反应、攻击优劣势、束缚敏捷豁免劣势、麻痹／石化／震慑／昏迷的力量／敏捷豁免自动失败、石化抗性与状态免疫。来源可见性、主动靠近与 5 尺自动重击提供查询函数，仍需地图层提交真实可见性／距离。房间法术 JSON 升至兼容 V1 的 schema V2，新增伤害、豁免、标准状态与升环纯数据字段；插件 API 新增 `registerSpell`、`dealDamage` 和 `applyStandardCondition`，普通 JSON 仍永远不能执行。
- 术士超魔法已有谨慎法术、远距法术、强化法术、升阶法术、瞬发法术、无声法术和孪生法术的地图选择、总术法点预算与 DM Headless 复核；孪生法术只接受当前环位下只能指定一个生物的法术，要求两个不同合法目标，为每个目标独立结算攻击骰、豁免和伤害，术法点消耗为当前施法环级（戏法为1点）。强化法术会在伤害骰公开后通过共享中断让施法者选择至多魅力调整值枚骰，由 DM 重掷且必须采用新结果；它可与另一种超魔法同时使用，跨孪生法术的多个伤害组共用同一个骰数上限。延时法术的双倍专注时长规则已在引擎中实现，但当前机械化术士法术尚无可用的持续型候选。龙族血脉6级“元素亲和”的伤害加值及可选抗性均已接入：同源法术可额外消耗1术法点获得对应抗性1小时，类型、等级、血脉、法术与“超魔法＋抗性”总费用均由 Headless 复核，状态通过角色存档和多端快照持久化。
- 龙族血脉14级“龙翼”会用附赠动作展开／收回并检查护甲，18级“龙威”会用动作消耗5术法点、建立1分钟专注，并在60尺敌对目标回合开始时由DM结算感知豁免、魅惑／恐慌及成功后的24小时同源免疫。2D地图只持久化飞行状态，立体高度与飞行路径仍由DM裁定。
- 32项 SRD 5.1 魔能祈唤全部可选，人物卡会阻止不满足等级、契约恩赐或已知魔能爆前提的新增选择；“迷人影响”会实际授予欺瞒与游说熟练。魔能爆已作为独立多射线 Headless 法术接入，5／11／17级分别产生2／3／4道独立攻击；苦痛魔爆逐道加入魅力，魔能长枪改为300尺，斥力魔爆逐道推动并由DM地图桥检查边界与障碍。依赖未机械化法术、魔宠、仪式、文字／感官、变形或隐形系统的祈唤只显示规则数据。
- 牧师“驱散亡灵／摧毁亡灵”已接入地图与 Headless：DM 权威快照自动枚举30尺内存活亡灵并逐个结算感知豁免，怪物 CR 直接来自 SRD stat block；5／8／11／14／17级分别摧毁 CR 1/2／1／2／3／4 以下的失败目标。被驱散状态持久化到角色或未关联 Token，禁止攻击和反应、禁止靠近来源，怪物自动回合会使用疾走逃离；受到任意实际伤害或10个自身回合后结束。
- 生命领域的生命门徒、维持生命、受祝福医者、神圣打击与最高治疗均已进入现有治疗／伤害事务；神迹干涉由DM权威 d100 或20级自动成功结算，成功后七日冷却通过房间战役日历跨日推进，具体神迹仍由DM决定。奉献圣武士的圣疗除治疗外还可按5点池移除疾病或中和毒素，驱散邪魔、纯净灵魂与神圣光轮也已进入Headless；神圣光轮持续10轮并在敌人回合开始造成10点光耀伤害。
- `src/rulesets/dnd5e/spellCatalog.ts` 按官方 SRD 5.1 职业法术表保存319个唯一法术及778条职业归属，测试固定吟游112、牧师105、德鲁伊105、圣武士31、游侠37、术士120、邪术师64、法师204。目录归属和“已机械化”是两个独立状态，未实现效果不得进入地图施放按钮。
- 圣武士守护灵光已在每次 Headless 地图快照中按阵营、距离和最高魅力加值自动应用；勇气灵光与奉献之誓的奉献灵光也会赋予对应状态免疫，18级范围扩大到30尺，昏迷或震慑的圣武士不投射灵光。
- 尚未完成：完整 SRD 5.1 法术效果、任意相连多立方体／墙体范围、其余反应与持续区域效果、死亡豁免地图事务、延时超魔法可用的术士持续法术、秘法奥秘中尚未机械化的复杂法术，以及依赖探索、语言、年龄、立体移动、魔宠、仪式、法术书抄录或通用感知系统的职业效果。上述效果只能显示规则文字或交由DM裁定；不得把“职业数据已收录”标成“所有效果已自动结算”。
- SRD 5.1 action router 已禁止玩家移动、怪物回合、攻击与施法进入旧 AP resolver；旧 `attack-token`、`aoe-attack`、`activate-feature` 和 `use-skill` 动作信封现在会在 DM 权威路由层直接归类为不支持，不会再分派到旧 AP 事务。共享战斗协议和页面运行时已删除敌方 AP 状态，不再发布、恢复或同步余额。旧存档会清空旧职业特性、AP 技能及旧战斗 Buff，历史日志会清除 AP 支出／余额文字。地图页已经删除旧 AP 闪避弹窗和选择处理，并拒绝非 `srd-5.1:` 怪物进入旧攻击结算。`MapsPage` 中不可达的旧敌人 AP 移动和旧借机攻击函数已经物理删除；`MapsPage` 与 `src/lib` 仍有其他未被 5e action router 调用的旧事务实现，后续要继续按依赖批次物理删除，不能把“当前不可触达”等同于源码已经全部清空。

## 角色创建推荐

- “初出茅庐”向导已从六个粗粒度选项扩展为十题。七个玩法维度实际参与全部 12 个 SRD 职业的加权排名，种族气质和两条阵营轴分别决定种族偏好与九宫格阵营。
- 问卷顶部实时显示前三职业和匹配度；“侦察与探索”等任何职业问题都会立即重算，不再只是装饰性选择。默认画像首选战士，把队伍定位单独改为侦察探索会切换为游侠。
- 最终确认页会按当前职业主属性和实际基础加点重新计算种族建议，列出前三名，并提示当前种族是否为数值首选。2014 固定种族调整和插件种族的固定／弹性调整都使用同一分析入口。
- 创建角色时会把职业匹配理由、主属性检查、当前种族理由和前三种族写入结构化 `dnd5eCreationRecommendation`，同时写入可编辑的“角色笔记”。这些信息不参与 AC、攻击、豁免或 Headless 战斗结算。
- 单元测试固定了“保护队友→战士、侦察探索→游侠”的排序变化，以及力量／敏捷战士会得到不同种族建议；E2E 覆盖实时变化、最终提示和创建后推荐理由持久化。

## 战斗结算模式

- 地图战斗控制栏已支持 DM 权威的“自动结算／手动结算／半自动”三档，并通过 `SharedCombatState.settlementMode` 同步；旧共享快照默认迁移为自动模式。
- 自动模式保持玩家动作与怪物 AI 全部经过 2014 5e Headless。手动模式关闭双方自动事务，玩家与 DM 使用手动骰盘，只有 DM 能应用 HP 结果和推进先攻。半自动模式保留玩家 Headless，暂停怪物 AI，由 DM 操作怪物。
- 手动骰盘支持 d4／d6／d8／d10／d12／d20／d100、1–12 枚骰和正负加值。公开投骰同步预定骰面和结果；DM 暗骰完全留在本地，不写共享事件或战斗日志，玩家共享消费层也会拒绝私密骰事件。
- DM 手动 HP 入口支持伤害、治疗和角色临时生命值；伤害先消耗临时生命值，治疗封顶，新临时生命值不叠加而取较高值。该入口是 DM 显式裁定，不恢复旧 AP 或攻防差值结算。
- 模式切换会清除待执行的怪物定时任务；怪物每个异步步骤提交前还会检查当前仍为自动模式，避免切换到手动后残留攻击落地。

## 法术书与房间法术导入

- 侧栏“法术书”是房间级法术资料库。核心目录来自 SRD 5.1 的 319 个唯一法术；“目录已收录”和“Headless 已机械化”是独立状态，界面会明确标识，未机械化法术不能进入地图自动施法按钮。
- `spellNamesZh.ts` 为 319 个 SRD 5.1 目录条目提供统一中文显示名，术语与用户提供的中文版法术表交叉核对。仅对应 SRD 已有条目；SRD 已通用化的法术继续使用无专有角色名的通用译名。英文 ID、职业归属、存档键和 Headless action ID 保持不变。
- `spellDescriptionsZh.generated.ts` 保存上述 319 个核心条目的中文法术资料，包括学派、仪式、施法时间、射程、成分、持续时间、规则正文、升环效果和 PDF 来源页。数据由 `scripts/extract-phb-srd-spells.py` 从用户提供的《玩家手册》中译本第 211–289 页生成，并以 `spellCatalog.ts` 的 SRD 5.1 ID／环级作为严格白名单：PDF 共识别 360 个法术标题，只接纳其中正好 319 个 SRD 条目，非 SRD 法术不会进入核心目录。重新生成时必须保留目录数量、ID 集合和环级一致性测试。
- 法术中文正文与 Headless 机械化状态保持分离。319 个条目都可在法术书查看中文资料，但只有已注册 Headless action 的法术可进入自动战斗结算；其他条目会明确显示“DM 裁定”，不得因为正文齐全而标成已自动结算。SRD 通用名与 PHB 专名不同的条目仍使用 SRD 通用显示名，并在来源行显示 PDF 中的原始标题以便核对。
- 施法职业的人物卡新增独立“法术书”页签，会按职业与当前可用环级筛选戏法和法术，并分别保存戏法、已知／已准备法术。法师额外保存自己的法术书清单，准备法术只能从该清单中选择；抄录来源、时间和费用目前仍交由 DM 裁定。
- 人物卡法术页已把“已学习戏法”“法术书中的已学习法术”“今日已准备／已知法术”和“所有可选法术”拆成独立区域；已选择条目不再埋在完整目录中。法术卡会显示该法术的全部 SRD 职业归属，点击卡片可打开包含完整中文正文、升环、来源页和 Headless 状态的详情对话框。角色存档键仍沿用 `spell-cantrips`、`wizard-spellbook`、`spell-prepared`／`spell-known`，没有迁移或重写已有存档。
- `docs/dnd5e-spell-headless-audit.md` 固定法术机械化边界：当前 34 个法术注册为 Headless，并按“复用现有模型／短期状态与特殊数值／Interrupt 或地图桥／需要大型子系统”列出后续接入批次。详情对话框通过 `document.body` Portal 挂载，必须相对浏览器视口居中，不能受人物卡滚动容器或变换上下文影响。
- DM 可在法术书页下载 `public/spell-templates/dnd5e-2014-spell-template.json`，然后导入 `.json` 或 `.dndstars5e-spells` 文件。模板覆盖命名空间 ID、名称、环级、八大学派、仪式、施法时间、射程／范围形状、V/S/M 成分、材料费用与消耗、持续时间／专注、职业、描述、升环、来源、发布者和许可证。
- 导入包固定为 `dndstars5e-spells` schema v1，限制 2 MiB／500 条法术，并禁止冒充核心命名空间。只有当前房间 DM 可以导入、替换、移除或导出房间法术；玩家加入房间后通过既有共享资源 revision／SSE 链自动收到相同资料。
- 普通法术 JSON 永远是 `reference-only`，不能包含或执行 JavaScript，也不能获得 DOM、网络、存储、内部 Store 或 Headless capability。它可以由 DM 通过裁定事务手工填写白名单效果；若要自动计算伤害、豁免、目标、状态或持续效果，仍必须通过经过沙箱、房间白名单与 DM 权威校验的 Rules Plugin API 注册专门行动，不能把文字模板当作可执行规则。
- 已新增裁定行动事务：人物卡已选择但尚未机械化的动作／附赠动作法术会显示在地图“需要 DM 裁定的法术”区域。玩家只提交法术 ID 与环位；DM 通过 `dm-adjudication` Interrupt 填写最终伤害、治疗、临时 HP、状态增删、专注轮数和备注。只有批准后，`adjudicated-spell` Headless action 才会原子消费动作／附赠动作及普通法术位、契约位、秘法奥秘或法术精通／招牌法术资源并应用效果。取消、超时、回合变化和最终复检失败不消费；反应法术仍等待专用触发器。
- 房间共享法术在载入时会逐条重跑同一模板校验；坏数据会进入共享完整性隔离提示，不会污染当前法术书。战役快照、导出与恢复会因其通用共享资源机制自动包含 `spellbook`。

## SRD 怪物接入状态

- `src/rulesets/dnd5e/monsters.ts` 是新怪物数据的规则真相源，ID 统一使用 `srd-5.1:` 命名空间。
- 地图怪物选择器只展示 SRD 目录；旧 `ENEMY_POOL` 与旧 stat block 继续存在仅为旧存档兼容，不得用于新增 5e 内容。
- 首批 10 个怪物：强盗、狗头人、哥布林、骷髅、僵尸、狼、兽人、恐狼、食人魔、枭熊。
- 地图桥接会把 SRD Token 转成包含真实 AC、HP、六属性、熟练加值、速度、伤害易伤／抗性／免疫和状态免疫的 `Dnd5eCombatant`。
- `monster-action` 由 5e Headless 根据 actor 的 stat block 反查命中加值和伤害骰；客户端不能提交自定义攻击修正。枭熊多重攻击作为一个动作依次结算喙击与爪击。
- SRD 怪物使用 `monsterTurnPlanner.ts` 按各自速度规划移动，并在同一回合执行至多一个 5e 动作；该路径不读取、不消费旧 `enemyApByToken`，也不会因旧 AP 余额追加第二次攻击。
- 怪物自动移动的 Headless 结果会由 DM 立即发布到房间地图，不能只写本地 Authority Store 后被旧共享快照回弹。二维地图会在步行与飞行速度中采用可用的较高速度；路径规划可绕过 Token 和障碍物。只有“自动结算”会运行怪物 AI；“半自动”仍按设计由 DM 手动拖动怪物。
- 尚未实现的 SRD 怪物动作效果（例如击倒豁免、亡灵坚韧、充能、法术、传奇动作）只能显示规则文本，不能标记为已自动结算；后续按结构化效果逐项补齐。

## 授权要求

默认只使用对应 SRD 中按 CC-BY-4.0 发布的内容，并在仓库加入准确署名。不要自行复制非 SRD 规则书、
D&D Beyond Basic Rules、专有世界观、图片或其他未授权内容。Rules Plugin API 只是技术扩展边界，不会替插件发布者取得内容授权，也不能作为平台托管或分发未授权内容的依据。

## 新对话启动提示

打开本仓库后，对 Codex 发送：

> 请先阅读 `HANDOFF.md`、`docs/class-extension.md` 和 `docs/combat-flow.md`，检查当前 Git 状态与测试，
> 然后继续建立 D&D 5e RulesetAdapter。使用 D&D 5e 2014（SRD 5.1）规则；保留 DM 权威、多端同步、
> 骰子与地图框架，不要沿用旧项目的 AP 和攻防差值伤害规则。先给出迁移顺序，再实现第一阶段。
